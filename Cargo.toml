[workspace]
resolver = "3"
members = [
    "flow-bot-onebot11",
    "flow-bot-extractor",
    "flow-bot-plugin-sdk",
    "flow-bot",
]
exclude = ["xtask"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
